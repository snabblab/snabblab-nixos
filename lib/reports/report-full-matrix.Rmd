---
title: "NFV test matrix"
output:
  html_document:
    toc: true
    toc_float: true
    toc_depth: 3
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(error = TRUE)
knitr::opts_chunk$set(fig.width = 10, fig.height = 8)
```

```{r}
# Setup

library(ggplot2)

d <- read.csv("bench.csv")
d$id <- as.factor(d$id)
d$pktsize <- as.factor(d$pktsize)

overall <- subset(as.data.frame(d), subset = benchmark == 'iperf' | benchmark == 'l2fwd')

iperf <- droplevels(subset(overall, subset = benchmark == 'iperf'))
l2fwd <- droplevels(subset(overall, subset = benchmark == 'l2fwd'))

# data frames with NA scores (failures) replaced with 0
overall0 <- data.frame(overall)
overall0$score[is.na(overall$score)] <- 0

iperf0 <- data.frame(iperf)
iperf0$score[is.na(iperf0$score)] <- 0

l2fwd0 <- data.frame(l2fwd)
l2fwd0$score[is.na(l2fwd0$score)] <- 0
```

Performance comparison between Snabb versions. Compares scores with various benchmarks, workloads, configurations, and software dependency versions. Results are reported as [probability density graphs](https://en.wikipedia.org/wiki/Probability_density_function) showing the distribution of benchmark scores (measured in Gbps for iperf and Mpps for l2fwd).

The overall purpose of this report is to highlight the relative strengths and weaknesses between Snabb software versions. This is particularly intended for evaluating how experimental changes affect performance and reliability.

This report starts with a very broad summary and then drills down on iperf and l2fwd results separately.

## Overall summary

```{r fig.width=8, fig.height=4}
ggplot(overall0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + ggtitle("Overall")
```

### Split by benchmark

```{r fig.width=8, fig.height=6}
ggplot(overall0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~benchmark, scales = "free", ncol = 1) + ggtitle("By benchmark")
```

## iperf

```{r fig.width=8, fig.height=4}
ggplot(iperf0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + ggtitle("iperf overall")
```

### Configuration

```{r}
ggplot(iperf0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~config, ncol=2) + ggtitle("iperf by configuration")
```

### Guest kernel

```{r}
ggplot(iperf0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~kernel, ncol=2) + ggtitle("iperf by guest kernel")
```

### QEMU

```{r}
ggplot(iperf0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~qemu, ncol=2) + ggtitle("iperf by qemu")
```

### Count of success and failure

Sucesses:

```{r}
summary(subset(iperf, subset = !is.na(score), select = c(config, qemu, kernel)))
```

Failures:

```{r}
summary(subset(iperf, subset = is.na(score), select = c(config, qemu, kernel)))
```

## l2fwd

```{r fig.width=8, fig.height=4}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + ggtitle("l2fwd overall")
```

### Packet size

```{r fig.width=8, fig.height=6}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~pktsize, ncol=1) + ggtitle("l2fwd by packet size")
```

### Configuration

```{r}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~config, ncol=2) + ggtitle("l2fwd by configuration")
```

### DPDK

```{r}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~dpdk, ncol=2) + ggtitle("l2fwd by DPDK")
```

### DPDK + configuration

```{r}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(dpdk ~ config) + ggtitle("l2fwd by DPDK and configuration")
```

### QEMU

```{r}
ggplot(l2fwd0, aes(score, color=snabb)) + theme(legend.position="top") + geom_density() + facet_wrap(~qemu, ncol=2) + ggtitle("l2fwd by QEMU")
```

### Count of success and failure

Sucesses:

```{r}
summary(subset(l2fwd, subset = !is.na(score), select = c(config, qemu, dpdk, snabb, pktsize)))
```

Failures:

```{r}
summary(subset(l2fwd, subset = is.na(score), select = c(config, qemu, dpdk, snabb, pktsize)))
```
